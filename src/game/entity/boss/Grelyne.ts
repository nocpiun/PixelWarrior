import Boss from "./Boss";

import { EntityAnimation } from "../../../types";

export default class Grelyne extends Boss {
    public playAnimation(animation: EntityAnimation): void {
        
    }
}
